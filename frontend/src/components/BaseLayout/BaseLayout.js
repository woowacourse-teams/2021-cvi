import { Background, Container } from './BaseLayout.styles';

const BaseLayout = () => {
  return (
    <Background>
      <Container></Container>
    </Background>
  );
};

export default BaseLayout;
